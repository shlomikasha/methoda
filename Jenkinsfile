pipeline {
    agent any

    stages {
        stage('Merge Branch') {
            steps {
                // Checkout the Git repository and merge the branch into master
                echo 'Woof'
            }
        }
    }
    }
